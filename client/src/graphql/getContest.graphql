query getContest($contestId: String!) {
  getContest(contestId: $contestId) {
    slug
    title
    body
    prizes
    participants
    entries
    header
    isFeatured
    # createdBy
    createdAt
  }
}
